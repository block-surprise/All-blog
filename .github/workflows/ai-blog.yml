name: AI Blog Generator

permissions:
  contents: write

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # =====================
      # リポジトリ取得
      # =====================
      - name: Checkout repo
        uses: actions/checkout@v4

      # =====================
      # Pythonセットアップ
      # =====================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # =====================
      # 依存関係インストール
      # =====================
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install google-generativeai feedparser requests markdown

      # =====================
      # 記事生成
      # =====================
      - name: Generate article
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: python generate.py

      # =====================
      # ホーム更新
      # =====================
      - name: Update homepage
        run: python .github/workflows/update_home.py

      # =====================
      # sitemap生成
      # =====================
      - name: Generate sitemap
        run: python .github/workflows/generate_sitemap.py

      # =====================
      # commit & push
      # =====================
      - name: Commit and push
        run: |
          git config user.name "ai-bot"
          git config user.email "bot@example.com"
          git add posts search index.html .github/workflows/*.yml
          git commit -m "auto update blog" || echo "no changes"
          git push
